name: Deploy Dev

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:     
    # - name: Check out repository
    #   uses: actions/checkout@v3   

    - name: Helm Install
      uses: ./.github/workflows/helm_install_template.yml
      with:
        region: 'us-east-1'
        cluster_name: 'qa'
